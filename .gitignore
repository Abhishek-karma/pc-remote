# --- .NET (windows-agent) ---
bin/
obj/
*.user
.vs/
.vscode/
TestResults/
*.trx
*.coverage
*.coveragexml

# Published startup exe and dist builds (don't track)
windows-agent/publish/
windows-agent/dist/

# --- Android (android-app) ---
.gradle/
build/
local.properties
captures/
*.apk
*.aab
.kotlin/
*.iml
.idea/

# --- Misc ---
.zcode/
.freebuff/
.DS_Store
Thumbs.db
*.log

# NuGet/package caches
packages/

# --- Development-only tooling (not part of the product) ---
.agents/
skills-lock.json
.claude/

# --- Local-only documentation (specs kept out of the public repository) ---
docs/

# --- Signing material (never commit) ---
*.jks
*.keystore

# --- Release signing material (local only, never commit) ---
signing/
